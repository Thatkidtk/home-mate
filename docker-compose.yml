
services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./instance:/app/instance
      - ./uploads:/app/uploads
      - ./.env:/app/.env:ro
    restart: unless-stopped
